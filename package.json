{
  "name": "cybershield-ai",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND -c cyan,green \"npm --prefix frontend run dev\" \"python -m waitress --listen=127.0.0.1:5000 backend.app:app\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "python -m waitress --listen=127.0.0.1:5000 backend.app:app"
  }
}